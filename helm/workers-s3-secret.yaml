# Optional second file: helm upgrade --install ... -f helm/airflow-values.yaml -f helm/workers-s3-secret.yaml
# Requires Secret aws-s3-creds in the same namespace with keys matching env var names.
workers:
  extraEnvFrom:
    - secretRef:
        name: aws-s3-creds
